# User Groups

**List groups:**
```bash
cat /etc/group
```
**Show groups of a user:**
```bash
groups username
```
**Add a group:**
```bash
sudo groupadd groupname
```
**Add a user to a group:**
```bash
sudo usermod -aG groupname username
```
